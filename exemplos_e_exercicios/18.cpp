
	#include<iostream>
	using namespace std;
	int main()
	
	{
	
	unsigned int Numero;   // Indicamos para o programa que a variável em questão é sempre positiva, com isso ele 'move' o intervalo de valores que ela pode assumir
	
	return 0;
	
	}
	
	// Exemplo: uma distância muito grande é um valor positivo mas que ultrapassa a capacidade do int. Usamos o modificador de variável 'usingned' assim estamos indicando para o programa que a variável é sempre positiva, com isso, ele "move" o intervalo de valores que ela pode assumir para cobrir apenas valores positivos. Então o limite máximo é aumentado.
	
	// Fonte: Responde Aí - Memória e Modificadores
